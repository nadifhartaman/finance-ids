import PageStub from "@/app/components/dashboard/PageStub";

export default function ProjectsPage() {
  return (
    <PageStub
      title="Projects"
      subtitle="Active projects, what is close to billing, and what is over budget."
      planned="A list of active projects with contract value, progress toward billing, and budget health."
    />
  );
}
