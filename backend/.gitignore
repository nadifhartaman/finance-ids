# dependencies
node_modules/

# env files — secrets never go in git; keep a committed .env.example instead
.env
.env.*
!.env.example

# logs
logs/
*.log
npm-debug.log*

# build output (if you add a build step later)
dist/
build/

# test coverage
coverage/

# supabase CLI machine-local state (project link, caches) — not schema code;
# migrations/ and seed.sql ARE committed
supabase/.temp/

# misc
.DS_Store

CLAUDE.md